/* Auto-generated by {{ generator_name }} from {{ source_document }}. */
#ifndef {{ include_guard }}
#define {{ include_guard }}

#include <stddef.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef struct {{ struct_name }} {
{% for field in fields -%}
    {{ field.type_info.render_declaration(field.name) }}{% if field.description %} /* {{ field.description }} */{% endif %}
{% endfor -%}
} {{ struct_name }};

int {{ pack_function }}(const {{ struct_name }} *msg, uint8_t *buf, size_t buf_len);
int {{ unpack_function }}({{ struct_name }} *msg, const uint8_t *buf, size_t buf_len);

#ifdef __cplusplus
}
#endif

#endif /* {{ include_guard }} */
