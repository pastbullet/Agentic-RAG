/* Auto-generated by {{ generator_name }} from {{ source_document }}. */
#ifndef {{ include_guard }}
#define {{ include_guard }}

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

typedef enum {{ state_enum }} {
{% for state in states -%}
    {{ state.name }}{{ ',' if not loop.last else '' }}{% if state.description %} /* {{ state.description }} */{% endif %}
{% endfor -%}
} {{ state_enum }};

typedef enum {{ event_enum }} {
{% for event in events -%}
    {{ event }}{{ ',' if not loop.last else '' }}
{% endfor -%}
} {{ event_enum }};

{{ state_enum }} {{ function_name }}({{ state_enum }} current_state, {{ event_enum }} event);

#ifdef __cplusplus
}
#endif

#endif /* {{ include_guard }} */
